/*EXERCÍCIO
Dica rápida, frases (string) devem ser colocadas entre aspas (simples ou duplas) 
e números não levam aspas.
*/

// Declare uma variável contendo uma string

var nome = "Gabi";

// Declare uma variável contendo um número dentro de uma string

var idade = "29";

// Declare uma variável com a sua idade

var idade2 = 29;

// Declare duas variáveis, uma com seu nome
// e outra com seu sobrenome e some as mesmas

var meuNome = "Gabi";
var sobrenome = "Deutner";
var nomeCompleto = meuNome + ' ' + sobrenome;

// Coloque a seguinte frase em uma variável: It's time

var frase = "It's time";

// Verifique o tipo da variável que contém o seu nome

console.log(typeof nome);

console.log(nome,idade, idade2, nomeCompleto, frase);